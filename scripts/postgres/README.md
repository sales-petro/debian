# PostgreSQL (`scripts/postgres/`)

| Script | Descrição |
|--------|-----------|
| `setup-postgres-remote.sh` | escuta `0.0.0.0:5432` (cluster online via `pg_lsclusters`) |
| `diag-postgres.sh` | diagnóstico conexão |

pgAdmin: host `192.168.100.220`, db `hubsaas`, user `postgres`.
